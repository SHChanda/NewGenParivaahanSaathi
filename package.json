{
  "name": "sarathi-next-demo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply sarathi-next-demo --local",
    "d1:migrate:remote": "wrangler d1 migrations apply sarathi-next-demo --remote"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
